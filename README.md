cram steals arguments. no kitty! bad kitty! those are my tasks kitty!
=====================================================================

```bash
git clone https://github.com/gogamoga/gulp-cram-fails.git
cd gulp-cram-fails
npm install
gulp
gulp foo
gulp bar
gulp foobar
```
